# Raspberry Pi 영어 학습 챗봇 구현 계획

## 1. 최종 목표

Raspberry Pi 5에서 Docker Compose로 실행되며, PC와 휴대폰 브라우저에서 이용할 수 있는 개인용 로컬 영어 학습 챗봇을 만든다.

- 로컬 LLM은 Ollama를 통해 실행한다.
- Python Agent가 프롬프트 구성, 파일 도구, 채팅 저장을 담당한다.
- 채팅과 메시지는 SQLite에 저장한다.
- 학습 프로필, 단어장, 이력 등 장기 학습 자료는 `workspace/`에 보존한다.
- LLM의 파일 접근은 `workspace/` 내부로만 제한한다.
- 각 구현 단계는 사용자 확인 후 하나씩 진행한다.

## 2. 목표 디렉터리 구조

```text
english_study_ai/
├── AGENTS.md                 # Codex 개발 지침
├── plan.ai.md                # 단계별 구현 계획과 진행 상태
├── README.md                 # 설치, 실행, 운영 방법
├── TODO.md                   # 현재 단계의 세부 작업 목록
├── .env.example              # 환경 변수 예시
├── .gitignore
├── docker-compose.yml
├── agent/
│   ├── Dockerfile
│   ├── pyproject.toml
│   ├── app/
│   │   ├── main.py           # API 및 애플리케이션 진입점
│   │   ├── config.py
│   │   ├── agent.py          # LLM 및 도구 실행 조정
│   │   ├── llm.py            # Ollama 어댑터
│   │   ├── db.py
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── services/
│   │   └── tools/
│   └── tests/
├── web/                      # 자체 Web UI를 선택할 경우
├── data/                     # SQLite 등 런타임 데이터
├── docs/
│   ├── ARCHITECTURE.md
│   └── SECURITY.md
└── workspace/
    ├── AGENT.md              # 영어 교사 LLM의 런타임 지침
    ├── 영어_학습프로필.md
    ├── 영어_복습단어.md
    ├── 영어_학습이력.md
    └── 영어_가이드라인.md
```

아직 사용하지 않는 디렉터리와 파일은 해당 구현 단계에서 생성한다.

## 3. 단계별 계획

### 0단계 — 환경과 제품 정책 결정

**상태:** 완료 (2026-09-03)

구현에 영향을 주는 하드웨어와 제품 정책을 먼저 확정한다.

필요한 정보:

- Raspberry Pi 5 RAM 용량
- Raspberry Pi OS 종류와 버전, 64비트 여부
- 저장장치 종류와 남은 용량
- UI 방식: 자체 Web UI 또는 Open WebUI
- 단일 사용자 전제 여부
- 날짜별 기본 채팅 외에 추가 채팅을 허용할지 여부
- 원격 접속 범위: LAN 전용 또는 Tailscale 등 VPN 사용
- 첫 Ollama 모델 후보와 실제 Pi에서 허용 가능한 응답 속도

확인된 실행 환경 (2026-09-03):

- CPU 아키텍처: `aarch64` (ARM64)
- OS: Debian GNU/Linux 13.6 (trixie), 64비트
- RAM: 7.9 GiB, 확인 당시 가용 7.3 GiB
- Swap: 2.0 GiB
- 주 저장장치: Samsung SSD 970 EVO NVMe 465.8 GiB
- 루트 파일시스템: ext4 457 GiB, 가용 419 GiB
- Docker Engine 및 Docker Compose: 미설치
- 판단: 8GB Pi 환경이므로 첫 후보는 4B Q4급으로 제한하고 실제 메모리와 속도를 벤치마크한다.

산출물:

- 확정된 기술 선택과 운영 조건
- 1단계에서 사용할 Python 및 주요 라이브러리 버전

완료 기준:

- 1단계 프로젝트 골격을 만들기에 필요한 선택이 확정됨

확정된 기술 및 운영 정책:

| 항목 | 권장값 | 이유 | 확정 상태 |
|---|---|---|---|
| UI | 자체 Web UI | 날짜별 채팅과 학습 UX를 정확히 구현하기 쉬움 | 확정 |
| 사용자 | 단일 사용자 | 개인용 로컬 서비스의 MVP 범위에 적합 | 확정 |
| 채팅 정책 | 날짜별 기본 채팅 1개 + 추가 채팅 허용 | 기본 학습 흐름을 유지하면서 임시 대화를 허용함 | 확정 |
| 원격 접속 | LAN 우선, 이후 Tailscale | 공용 인터넷 직접 노출 없이 안전하게 접근 가능 | 확정 |
| API | FastAPI | 비동기 API, 검증, 문서화 및 Python 생태계가 적합 | 확정 |
| Python | `python:3.13` Docker 이미지 | Raspberry Pi와 컨테이너에서 Python 3.13 계열을 사용함 | 확정 |
| 테스트 | pytest | Python API와 보안 경계 테스트에 적합 | 확정 |
| DB | SQLite | 단일 사용자와 로컬 배포에 충분하며 운영이 단순함 | 확정 |
| 타임존 | Asia/Seoul | 날짜별 학습 채팅의 사용자 기준 시간대 | 확정 |
| LLM 실행 | Ollama | 로컬 모델 실행 및 모델 교체 요구사항에 부합 | 확정 |

첫 모델 및 성능 기준 제안:

- 우선 후보: Ollama의 `gemma3:4b`
- 대체 후보: 1B급 Gemma 모델(4B가 느리거나 메모리 부족일 때)
- 초기 벤치마크: 첫 응답 시작 시간, 전체 응답 시간, 메모리 최고 사용량 기록
- 제안 합격 기준: 첫 응답 시작 5초 이내, 약 80단어 답변 완료 30초 이내, swap 과다 사용 없음

하드웨어 확인 명령:

```bash
uname -m
cat /etc/os-release
free -h
df -h /
lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINTS,MODEL
docker --version
docker compose version
```

0단계 결정 기록:

- [x] Raspberry Pi CPU 아키텍처와 OS 확인
- [x] RAM과 저장장치 여유 공간 확인
- [x] Docker 및 Compose 설치 상태 확인 — 현재 미설치, 설치는 1단계 준비 작업
- [x] UI, 사용자, 채팅, 원격 접속 정책 확정
- [x] Python, API 프레임워크, 테스트 도구 확정
- [x] 첫 Ollama 모델 후보와 허용 응답 시간 확정

0단계 결과:

- 1단계는 `python:3.13` Docker 이미지, FastAPI, pytest 기반으로 시작한다.
- 의존성의 정확한 패치 버전은 1단계에서 ARM64 설치 및 테스트 후 잠근다.
- Docker Engine과 Compose 설치는 Raspberry Pi 배포 전에 필요하지만 로컬 프로젝트 골격 작성을 막지 않는다.
- `gemma3:4b`는 4단계에서 실제 Raspberry Pi 성능 기준을 통과해야 최종 기본 모델이 된다.

---

### 1단계 — 프로젝트 골격과 설정

**상태:** 대기

Python Agent가 실행될 수 있는 최소 프로젝트 구조를 만든다. 이 단계에서는 실제 LLM 대화를 구현하지 않는다.

#### 목적

1단계는 영어 교사 기능을 만드는 단계가 아니라, 이후 기능을 안전하게 올릴 수 있는 실행 기반을 준비하는 단계다. 집을 짓기 전에 골조, 전기 배선 위치, 점검 방법을 먼저 마련하는 것과 같다.

이 단계가 끝나면 작은 Python 웹 서버를 실행하고 상태를 확인할 수 있다. 설정이 잘못되면 프로그램이 시작할 때 원인을 알려 주며, 테스트로 기본 구조가 정상인지 반복해서 확인할 수 있다.

Ollama는 1단계에 필요하지 않다. `MODEL`과 `OLLAMA_BASE_URL` 설정 항목만 미리 정의하고, 실제 설치·연결·모델 대화는 4단계에서 진행한다. 따라서 Ollama가 현재 설치되어 있지 않아도 1단계를 학습하고 구현할 수 있다.

#### 각 작업의 의미

- `agent/` 패키지: Python 서버 코드와 테스트를 정리해 둘 기본 폴더다.
- FastAPI: 브라우저나 Web UI가 Python Agent에 요청을 보내는 통로인 API를 만든다.
- 상태 확인 API: 서버가 실행 중인지 확인하는 가장 작은 기능이다. 예를 들어 `GET /health` 요청에 `{"status": "ok"}`를 반환한다.
- 환경 변수: 모델명, 서버 주소, 파일 경로처럼 환경마다 달라지는 값을 코드 밖에서 바꾸는 방법이다.
- 설정 검증: 잘못된 주소나 빈 값 때문에 나중에 모호한 오류가 발생하지 않도록 시작 시점에 확인한다.
- 로깅: 서버가 시작됐는지, 어디에서 오류가 났는지를 터미널에 기록한다.
- pytest: 작성한 기능이 이후 변경으로 망가지지 않았는지 자동으로 확인한다.
- Docker 이미지: 개발 PC와 Raspberry Pi에서 가능한 한 같은 Python 실행 환경을 사용하게 한다.
- 문서와 예제 설정: 실행 방법과 필요한 값을 다음 작업에서도 다시 확인할 수 있게 한다.

필요한 것:

- ARM64를 지원하는 `python:3.13` 컨테이너 이미지(`slim` 제외)
- API 프레임워크: FastAPI(0단계에서 확정)
- 의존성 및 테스트 도구 선택
- 환경 변수 목록: `MODEL`, `OLLAMA_BASE_URL`, `WORKSPACE_PATH`, `DATABASE_URL`, `TZ`

#### 구현 전에 사용자와 확정할 사항

아래 항목은 코드를 작성하는 방법을 정하는 선택이다. AI가 임의로 확정하지 않고 구현 요청 전에 사용자에게 설명한 뒤 하나씩 결정한다.

1. **의존성 관리와 버전 고정 방법**
   - 의존성은 FastAPI처럼 프로젝트가 사용하는 외부 Python 프로그램을 뜻한다.
   - `pyproject.toml`은 필요한 의존성과 프로젝트 정보를 적는 파일이다.
   - 버전 고정은 다른 날 다시 설치해도 같은 버전을 사용하도록 기록하는 것이다.
   - 권장 방향: `pyproject.toml`로 관리하고, ARM64에서 설치와 테스트를 통과한 정확한 버전을 고정한다.
2. **환경 변수의 기본값과 필수값 — 확정**
   - 기본값이 있으면 값을 따로 입력하지 않아도 실행된다.
   - 필수값은 사용자가 반드시 입력해야 하며, 빠지면 프로그램이 이유를 설명하고 종료한다.
   - 모든 환경 변수에 안전한 기본값을 제공한다.
   - 빈 값이나 형식이 잘못된 값은 명확한 오류로 거부한다.
3. **공식 실행 방법 — 확정**
   - 호스트 실행은 현재 컴퓨터에 설치된 Python으로 직접 실행하는 방식이다.
   - Docker 실행은 준비된 컨테이너 안에서 실행해 PC와 Raspberry Pi의 환경 차이를 줄이는 방식이다.
   - Docker를 공식 실행 방법으로 삼고, 학습과 빠른 테스트를 위한 호스트 실행 방법도 문서화한다.
4. **상태 확인 API의 약속**
   - API 주소, 성공 응답 형식, 검사 범위를 미리 정해야 테스트 기준이 분명해진다.
   - 권장 방향: `GET /health`가 HTTP 200과 `{"status": "ok"}`를 반환한다.
   - 1단계에서는 Python 서버만 확인하며 Ollama, SQLite, workspace 연결은 검사하지 않는다.
5. **설정 검증 범위**
   - URL 형식, 빈 모델명, workspace 경로, SQLite 주소, 타임존 값 중 무엇을 검사할지 정한다.
   - 1단계에서는 실제 Ollama 연결이나 DB 파일 생성을 하지 않고 설정값의 형식만 검사한다.
6. **로그 형식**
   - 권장 방향: 터미널 표준 출력에 시간, 로그 수준, 메시지를 기록하고 기본 수준은 `INFO`로 한다.
   - 비밀번호나 대화 내용 같은 민감한 값은 로그에 남기지 않는다.

구현 항목:

- `agent/` 패키지와 테스트 구조
- 설정 로딩 및 필수 설정 검증
- 기본 상태 확인 API
- `README.md`, `.env.example`, `.gitignore`, `TODO.md`
- 기본 로깅

완료 기준:

- Ollama 없이 로컬 또는 Docker에서 앱이 시작됨
- 상태 확인 API가 합의한 응답을 반환하고 자동 테스트를 통과함
- 잘못된 필수 설정에 원인을 알 수 있는 오류를 반환함
- 테스트 실행에 Ollama, SQLite DB 파일, 실제 workspace가 필요하지 않음

---

### 2단계 — workspace 로딩과 안전한 파일 도구

**상태:** 대기

LLM 연동 전에 가장 중요한 파일 접근 경계를 구현한다.

필요한 것:

- 허용할 파일 확장자와 최대 파일 크기
- 기존 파일 덮어쓰기 허용 정책
- `workspace/AGENT.md` 누락 시 시작 실패 정책 확인

구현 항목:

- 시작 시 `workspace/AGENT.md` 필수 로딩
- `list_files`, `read_file`, `write_file`, `append_file`
- 상대 경로만 허용
- 정규화된 실제 경로가 workspace 안에 있는지 검증
- `..`, 절대 경로, 심볼릭 링크 탈출 차단
- 텍스트 인코딩, 확장자, 크기 제한
- 가능한 경우 원자적 파일 쓰기

완료 기준:

- 정상적인 workspace 파일 읽기·쓰기 테스트 통과
- 경로 순회 및 심볼릭 링크 공격 테스트 통과
- workspace 밖 파일에 접근할 수 없음

---

### 3단계 — SQLite 채팅 저장

**상태:** 대기

모델과 UI에서 공통으로 사용할 채팅 및 메시지 저장 계층을 만든다.

필요한 것:

- 날짜당 채팅을 DB에서 정확히 하나로 제한할지 여부
- 추가 채팅의 날짜 및 제목 정책
- 채팅 이름 변경 기능 포함 여부
- 사용자 타임존 기본값

구현 항목:

- `chats`, `messages` 스키마와 마이그레이션
- 오늘 채팅 조회 또는 생성
- 채팅 생성, 목록, 조회, 삭제
- 사용자 및 assistant 메시지 저장
- 채팅 삭제 시 메시지 cascade 삭제
- 동시 요청 시 오늘 채팅 중복 방지

완료 기준:

- 재시작 후 채팅과 메시지가 복원됨
- 날짜 경계 테스트 통과
- 삭제 시 채팅 메시지만 제거되고 workspace 자료는 유지됨

---

### 4단계 — Ollama와 Python Agent 연동

**상태:** 대기

모델을 교체할 수 있는 추상화와 영어 학습용 대화 흐름을 구현한다.

필요한 것:

- Pi 환경에서 사용할 정확한 Ollama 모델 태그
- 모델의 tool calling 지원 방식 확인
- 네이티브 도구 호출 또는 구조화 JSON 프로토콜 선택
- 한 요청에 포함할 최근 대화 범위

구현 항목:

- Ollama 상태 및 모델 가용성 확인
- 교체 가능한 LLM 어댑터 인터페이스
- 런타임 지침, 최근 채팅, 필요한 학습 자료를 조합하는 프롬프트
- 검증된 파일 도구 실행 루프
- 모델 연결 및 도구 오류의 안전한 처리
- 요청과 응답의 DB 저장

완료 기준:

- 모델명을 환경 변수로 바꿀 수 있음
- 영어 대화와 교정 응답이 저장됨
- 허용된 도구만 호출됨
- Ollama 장애 시 사용자가 이해할 수 있는 오류를 반환함

---

### 5단계 — 반응형 Web UI

**상태:** 대기

PC와 휴대폰에서 사용할 채팅 화면을 만든다.

필요한 것:

- 0단계에서 결정한 UI 방식
- 지원할 최소 브라우저와 화면 크기
- 스트리밍 응답을 MVP에 포함할지 여부

구현 항목:

- 오늘 학습 자동 열기
- 메시지 입력과 응답 표시
- 날짜별 채팅 목록과 과거 채팅 열기
- 새 채팅 생성
- 확인 절차가 포함된 채팅 삭제
- 로딩, 오류, 빈 화면 상태
- 모바일 반응형 레이아웃

완료 기준:

- PC와 휴대폰에서 핵심 흐름이 작동함
- 새로고침 후 기존 채팅이 복원됨
- UI가 서버 파일이나 Ollama에 직접 접근하지 않음

---

### 6단계 — Docker Compose와 Raspberry Pi 배포

**상태:** 대기

Agent와 Ollama를 분리하고 데이터를 영속화한다.

필요한 것:

- Raspberry Pi SSH 또는 직접 실행 환경
- Docker Engine과 Compose 플러그인
- 모델 및 DB를 보관할 저장공간
- LAN/VPN 접근 방식

구현 항목:

- ARM64 호환 Agent 이미지
- `agent`, `ollama` 서비스 구성
- 모델, SQLite, workspace 영속 볼륨
- 비루트 컨테이너와 최소 권한
- health check 및 재시작 정책
- LAN 접속과 안전한 원격 접속 문서

완료 기준:

- Pi에서 `docker compose up -d --build`로 실행됨
- 컨테이너 재생성 후 DB, workspace, 모델이 유지됨
- 휴대폰에서 접속 가능함
- Docker 소켓이나 불필요한 호스트 경로가 노출되지 않음

---

### 7단계 — 통합 검증과 운영 준비

**상태:** 대기

MVP 전체 흐름을 검증하고 백업 및 장애 대응 방법을 문서화한다.

필요한 것:

- 허용 가능한 응답 시간과 메모리 사용량 기준
- SQLite와 workspace 백업 위치 및 주기
- 로그 보존 기간

구현 항목:

- 전체 사용자 흐름 통합 테스트
- 실제 Pi 성능과 메모리 측정
- 백업 및 복구 절차
- 데이터 손상 및 Ollama 장애 대응
- 설치·업데이트·운영 문서 완성

완료 기준:

- 주요 MVP 시나리오가 모두 통과함
- 백업에서 DB와 workspace를 복구할 수 있음
- 운영자가 설치, 시작, 중지, 업데이트, 문제 확인을 문서만 보고 수행할 수 있음

## 4. MVP 이후 후보

MVP 완료 전에는 기본 범위에 포함하지 않는다.

- 응답 스트리밍
- 오래된 대화 요약과 컨텍스트 예산 관리
- 음성 입력, STT, TTS
- 다중 사용자 및 인증
- HTTPS 공개 서비스
- Gemma와 다른 모델의 자동 벤치마크
- 학습 통계 및 시각화

## 5. 단계 진행 원칙

각 단계는 다음 순서로 진행한다.

1. 사용자와 해당 단계의 미결정 사항을 확인한다.
2. `TODO.md`에 그 단계의 작은 작업과 완료 조건을 작성한다.
3. 해당 단계 범위만 구현한다.
4. 자동 테스트와 필요한 수동 검증을 수행한다.
5. 이 문서에 상태, 실제 결과, 남은 문제를 기록한다.
6. 사용자 확인 후 다음 단계로 넘어간다.
