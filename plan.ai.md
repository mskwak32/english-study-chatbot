# Raspberry Pi 영어 학습 챗봇 구현 계획

## 1. 최종 목표

Raspberry Pi 5에서 Docker Compose로 실행되며, PC와 휴대폰 브라우저에서 이용할 수 있는 개인용 로컬 영어 학습 챗봇을 만든다.

- 로컬 LLM은 Ollama를 통해 실행한다.
- Python Agent가 프롬프트 구성, 파일 도구, 채팅 저장을 담당한다.
- 채팅과 메시지는 SQLite에 저장한다.
- 학습 프로필, 단어장, 이력 등 장기 학습 자료는 `workspace/`에 보존한다.
- LLM의 파일 접근은 `workspace/` 내부로만 제한한다.
- 각 구현 단계는 사용자 확인 후 하나씩 진행한다.

## 2. 목표 디렉터리 구조

```text
english_study_ai/
├── AGENTS.md                 # Codex 개발 지침
├── plan.ai.md                # 단계별 구현 계획과 진행 상태
├── README.md                 # 설치, 실행, 운영 방법
├── TODO.md                   # 현재 단계의 세부 작업 목록
├── .env.example              # 환경 변수 예시
├── .gitignore
├── docker-compose.yml
├── agent/
│   ├── Dockerfile
│   ├── pyproject.toml
│   ├── app/
│   │   ├── main.py           # API 및 애플리케이션 진입점
│   │   ├── config.py
│   │   ├── study_time.py     # UTC 시각 변환과 사용자 기준 학습 날짜 계산
│   │   ├── agent.py          # LLM 및 도구 실행 조정
│   │   ├── llm.py            # Ollama 어댑터
│   │   ├── database/          # SQLite 저장 계층
│   │   │   ├── __init__.py
│   │   │   ├── connection.py # URL 검증 및 DB 연결 생성
│   │   │   ├── schema.py     # 스키마 초기화와 마이그레이션
│   │   │   ├── chats.py      # 채팅 조회·생성·삭제
│   │   │   └── messages.py   # 메시지 저장·조회
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── services/         # 여러 계층을 조합하는 애플리케이션 작업 흐름
│   │   └── tools/            # LLM이 호출하는 제한된 작업 인터페이스
│   └── tests/
├── web/                      # 자체 Web UI를 선택할 경우
├── data/                     # SQLite 등 런타임 데이터
├── docs/
│   ├── ARCHITECTURE.md
│   └── SECURITY.md
└── workspace/
    ├── AGENT.md              # 영어 교사 LLM의 런타임 지침
    ├── 영어_학습프로필.md
    ├── 영어_복습단어.md
    ├── 영어_학습이력.md
    └── 영어_가이드라인.md
```

아직 사용하지 않는 디렉터리와 파일은 해당 구현 단계에서 생성한다.

## 3. 단계별 계획

### 0단계 — 환경과 제품 정책 결정

**상태:** 완료 (2026-09-03)

확인된 실행 환경:

- CPU 아키텍처: `aarch64` (ARM64)
- OS: Debian GNU/Linux 13.6 (trixie), 64비트
- RAM: 7.9 GiB, 확인 당시 가용 7.3 GiB
- Swap: 2.0 GiB
- 주 저장장치: Samsung SSD 970 EVO NVMe 465.8 GiB
- 루트 파일시스템: ext4 457 GiB, 가용 419 GiB
- Docker Engine 및 Docker Compose: 미설치

확정된 기술 및 운영 정책:

| 항목 | 권장값 | 이유 | 확정 상태 |
|---|---|---|---|
| UI | 자체 Web UI | 날짜별 채팅과 학습 UX를 정확히 구현하기 쉬움 | 확정 |
| 사용자 | 단일 사용자 | 개인용 로컬 서비스의 MVP 범위에 적합 | 확정 |
| 채팅 정책 | 날짜별 기본 채팅 1개 + 추가 채팅 허용 | 기본 학습 흐름을 유지하면서 임시 대화를 허용함 | 확정 |
| 원격 접속 | LAN 우선, 이후 DDNS와 포트 포워딩 | 외부 브라우저 접속 목표, 보안 기능 구현 후 적용 | 확정 |
| API | FastAPI | 비동기 API, 검증, 문서화 및 Python 생태계가 적합 | 확정 |
| Python | `python:3.13` Docker 이미지 | Raspberry Pi와 컨테이너에서 Python 3.13 계열을 사용함 | 확정 |
| 테스트 | pytest | Python API와 보안 경계 테스트에 적합 | 확정 |
| DB | SQLite | 단일 사용자와 로컬 배포에 충분하며 운영이 단순함 | 확정 |
| 타임존 | Asia/Seoul | 날짜별 학습 채팅의 사용자 기준 시간대 | 확정 |
| LLM 실행 | Ollama | 로컬 모델 실행 및 모델 교체 요구사항에 부합 | 확정 |

초기 모델 및 성능 기준:

- 우선 후보: Ollama의 `gemma3:4b`
- 대체 후보: 1B급 Gemma 모델(4B가 느리거나 메모리 부족일 때)
- 초기 벤치마크: 첫 응답 시작 시간, 전체 응답 시간, 메모리 최고 사용량 기록
- 제안 합격 기준: 첫 응답 시작 5초 이내, 약 80단어 답변 완료 30초 이내, swap 과다 사용 없음

- 1단계는 `python:3.13`, FastAPI, pytest 기반으로 시작한다.
- 의존성 버전은 ARM64 설치와 테스트 후 고정한다.
- `gemma3:4b`는 4단계의 Pi 성능 기준을 통과해야 기본 모델로 확정한다.

---

### 1단계 — 프로젝트 골격과 설정

**상태:** 완료 (2026-09-04)

LLM 연동 없이 Python Agent의 실행·설정 기반을 만든다.

구현 및 검증 결과:

- FastAPI 앱, `GET /health`, 설정 기본값·환경 변수·형식 검증, 기본 로깅 구현
- `.env` 지원, `.env.example`, `requirements.lock`, Dockerfile, `.dockerignore` 작성
- Python 3.13 로컬 테스트: `9 passed` (외부 의존성의 향후 변경 경고 2개)
- ARM64 Docker 이미지 빌드, 컨테이너 실행, `GET /health`의 HTTP 200 응답 확인

완료 기준:

- Ollama 없이 로컬과 Docker에서 앱이 시작됨
- 상태 확인 API가 합의한 응답을 반환하고 자동 테스트를 통과함
- 잘못된 설정에 원인을 알 수 있는 오류를 반환함
- 테스트 실행에 Ollama, SQLite DB 파일, 실제 workspace가 필요하지 않음

---

### 2단계 — workspace 로딩과 안전한 파일 도구

**상태:** 완료 (2026-09-07)

LLM 연동 전에 가장 중요한 파일 접근 경계를 구현한다.

필요한 것:

- 허용 확장자: `.md`, `.txt`
- 파일 최대 크기: 5 MiB
- 기존 파일 덮어쓰기: 허용하되 원자적으로 교체
- `workspace/AGENT.md` 누락 또는 읽기 실패 시 애플리케이션 시작 실패
- 신규 학습 자료 템플릿: 프로필, 학습 이력, 복습 단어 3종만 제공

구현 항목:

- 시작 시 `workspace/AGENT.md` 필수 로딩
- 누락된 학습 자료를 고정 템플릿으로 생성하되 기존 기록은 보존
- `list_files`, `read_file`, `write_file`, `append_file`
- 상대 경로만 허용
- 정규화된 실제 경로가 workspace 안에 있는지 검증
- `..`, 절대 경로, 심볼릭 링크 탈출 차단
- 텍스트 인코딩, 확장자, 크기 제한
- 가능한 경우 원자적 파일 쓰기

구현 및 검증 결과:

- 시작 시 `workspace/AGENT.md`를 필수로 읽고, 누락·빈 파일·UTF-8 오류를 시작 오류로 처리
- 누락된 학습 프로필·학습 이력·복습 단어 템플릿만 생성하고 기존 기록은 보존
- `list_files`, `read_file`, `write_file`, `append_file` 구현
- `.md`, `.txt` 확장자와 5 MiB 크기 제한 적용
- 절대 경로, `..` 경로 순회, workspace 밖 심볼릭 링크 접근 차단
- 임시 파일 작성 후 `os.replace()`로 파일을 원자적으로 교체
- 전체 테스트: `29 passed` (외부 라이브러리의 향후 API 변경 경고 2개)

완료 기준:

- 정상적인 workspace 파일 읽기·쓰기 테스트 통과
- 경로 순회 및 심볼릭 링크 공격 테스트 통과
- workspace 밖 파일에 접근할 수 없음

---

### 3단계 — SQLite 채팅 저장

**상태:** 완료 (2026-09-08)

모델과 UI에서 공통으로 사용할 채팅 및 메시지 저장 계층을 만든다.

확정한 정책:

- `Asia/Seoul` 날짜마다 기본 학습 채팅을 정확히 하나만 둔다.
- 추가 학습은 같은 날짜에 생성하며 제목은
  `{기본 제목} - 추가 학습 (N)` 형식으로 자동 생성한다. `N`은 1부터 시작한다.
- 추가 학습을 삭제한 뒤에는 비어 있는 번호를 다시 사용할 수 있다.
- 사용자가 채팅 제목을 직접 변경하는 기능은 이번 단계에 포함하지 않는다.

구현 항목:

- `chats`, `messages` 스키마와 마이그레이션
- 오늘 채팅 조회 또는 생성
- 채팅 생성, 목록, 조회, 삭제
- 사용자 및 assistant 메시지 저장
- 채팅 삭제 시 메시지 cascade 삭제
- 동시 요청 시 오늘 채팅 중복 방지

구현 및 검증 결과:

- 절대 경로 SQLite URL 검증과 연결별 외래 키 제약 활성화
- `schema_migrations`를 사용한 버전 1 스키마 마이그레이션 적용
- 날짜별 기본 채팅과 추가 학습 채팅의 생성·조회·목록·삭제 구현
- 삭제한 추가 학습 번호 재사용과 사용자 제목 변경 제외 정책 반영
- 사용자 및 assistant 메시지의 순서 보장 저장과 조회 구현
- `Asia/Seoul` 학습일 계산과 오늘의 기본 채팅 서비스 구현
- 앱 시작 시 데이터베이스 스키마 초기화
- 동시 기본 채팅·추가 채팅·메시지 요청의 중복 방지 검증
- 채팅 삭제 시 연결 메시지만 cascade 삭제되고 workspace 자료는 유지됨을 검증
- Ruff 포맷·린트 검사 통과
- 전체 테스트: `55 passed` (사용자 확인)

완료 기준:

- 재시작 후 채팅과 메시지가 복원됨
- 날짜 경계 테스트 통과
- 삭제 시 채팅 메시지만 제거되고 workspace 자료는 유지됨

---

### 4단계 — Ollama와 Python Agent 연동

**상태:** 대기

모델을 교체할 수 있는 추상화와 영어 학습용 대화 흐름을 구현한다.

필요한 것:

- Pi 환경에서 사용할 정확한 Ollama 모델 태그
- 모델의 tool calling 지원 방식 확인
- 네이티브 도구 호출 또는 구조화 JSON 프로토콜 선택
- 한 요청에 포함할 최근 대화 범위

구현 항목:

- Ollama 상태 및 모델 가용성 확인
- 교체 가능한 LLM 어댑터 인터페이스
- 런타임 지침, 최근 채팅, 필요한 학습 자료를 조합하는 프롬프트
- 검증된 파일 도구 실행 루프
- 모델 연결 및 도구 오류의 안전한 처리
- 요청과 응답의 DB 저장

완료 기준:

- 모델명을 환경 변수로 바꿀 수 있음
- 영어 대화와 교정 응답이 저장됨
- 허용된 도구만 호출됨
- Ollama 장애 시 사용자가 이해할 수 있는 오류를 반환함

---

### 5단계 — 반응형 Web UI

**상태:** 대기

PC와 휴대폰에서 사용할 채팅 화면을 만든다.

필요한 것:

- 0단계에서 결정한 UI 방식
- 지원할 최소 브라우저와 화면 크기
- 스트리밍 응답을 MVP에 포함할지 여부

구현 항목:

- 오늘 학습 자동 열기
- 메시지 입력과 응답 표시
- 날짜별 채팅 목록과 과거 채팅 열기
- 새 채팅 생성
- 확인 절차가 포함된 채팅 삭제
- 로딩, 오류, 빈 화면 상태
- 모바일 반응형 레이아웃

완료 기준:

- PC와 휴대폰에서 핵심 흐름이 작동함
- 새로고침 후 기존 채팅이 복원됨
- UI가 서버 파일이나 Ollama에 직접 접근하지 않음

---

### 6단계 — Docker Compose와 Raspberry Pi 배포

**상태:** 대기

Agent와 Ollama를 분리하고 데이터를 영속화한다.

필요한 것:

- Raspberry Pi SSH 또는 직접 실행 환경
- Docker Engine과 Compose 플러그인
- 모델 및 DB를 보관할 저장공간
- LAN/VPN 접근 방식

구현 항목:

- ARM64 호환 Agent 이미지
- `agent`, `ollama` 서비스 구성
- 모델, SQLite, workspace 영속 볼륨
- 비루트 컨테이너와 최소 권한
- health check 및 재시작 정책
- LAN 접속과 안전한 원격 접속 문서

완료 기준:

- Pi에서 `docker compose up -d --build`로 실행됨
- 컨테이너 재생성 후 DB, workspace, 모델이 유지됨
- 휴대폰에서 접속 가능함
- Docker 소켓이나 불필요한 호스트 경로가 노출되지 않음

---

### 7단계 — 통합 검증과 운영 준비

**상태:** 대기

MVP 전체 흐름을 검증하고 백업 및 장애 대응 방법을 문서화한다.

필요한 것:

- 허용 가능한 응답 시간과 메모리 사용량 기준
- SQLite와 workspace 백업 위치 및 주기
- 로그 보존 기간

구현 항목:

- 전체 사용자 흐름 통합 테스트
- 실제 Pi 성능과 메모리 측정
- 백업 및 복구 절차
- 데이터 손상 및 Ollama 장애 대응
- `README.md`를 포함한 설치·업데이트·운영 문서 완성

완료 기준:

- 주요 MVP 시나리오가 모두 통과함
- 백업에서 DB와 workspace를 복구할 수 있음
- 운영자가 설치, 시작, 중지, 업데이트, 문제 확인을 문서만 보고 수행할 수 있음

## 4. MVP 이후 후보

MVP 완료 전에는 기본 범위에 포함하지 않는다.

- 응답 스트리밍
- 오래된 대화 요약과 컨텍스트 예산 관리
- 음성 입력, STT, TTS
- 로그인과 사용자 계정 관리
- DDNS와 공유기 포트 포워딩을 통한 외부 브라우저 접속
- 리버스 프록시, HTTPS 인증서 발급·자동 갱신
- 외부 공개 접속의 접근 제어, 접속 기록, 보안 점검
- 다중 사용자 지원
- Gemma와 다른 모델의 자동 벤치마크
- 학습 통계 및 시각화

## 5. 단계 진행 원칙

각 단계는 다음 순서로 진행한다.

1. 사용자와 해당 단계의 미결정 사항을 확인한다.
2. `TODO.md`에 그 단계의 작은 작업과 완료 조건을 작성한다.
3. 해당 단계 범위만 구현한다.
4. 자동 테스트와 필요한 수동 검증을 수행한다.
5. 이 문서에 상태, 실제 결과, 남은 문제를 기록한다.
6. 완료한 단계의 `TODO.md` 항목을 삭제한다.
7. 사용자 확인 후 다음 단계로 넘어간다.
